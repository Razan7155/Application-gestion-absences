spring.application.name=absence

# PORT
server.port=${PORT:9094}

# DATABASE
spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost:3306/absence}
spring.datasource.username=${MYSQLUSER:Razan6}
spring.datasource.password=${MYSQLPASSWORD:razanelbarki66.}

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# JWT
jwt.secret=${JWT_SECRET:mySecretKey123456789}

# CORS
spring.mvc.cors.allowed-origins=*